[package]
name = "tsubaki-kernel"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
faer = { version = "0.24", default-features = false, features = ["linalg", "sparse"] }

[profile.release]
opt-level = 3
